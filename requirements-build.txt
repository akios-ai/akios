# Build dependencies for AKIOS
# These are required for creating standalone binaries and containers

# Binary packaging
PyInstaller>=5.0.0

# UPX for compression (optional but recommended)
# upx>=4.0.0  # Install system-wide for best results

# Docker for container builds (optional)
# docker>=6.0.0

# Build system
setuptools>=65.0.0
wheel>=0.37.0
build>=1.0.0

# Testing build artifacts (minimal for build verification)
pytest>=7.0.0
