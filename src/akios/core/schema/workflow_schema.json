{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://akios.ai/schemas/workflow/v1.1",
  "version": "1.2.0",
  "title": "AKIOS Workflow Schema",
  "description": "JSON schema for AKIOS workflow validation (v1.2 — adds parallel blocks, webhook/database agents, plugin support)",
  "type": "object",
  "required": ["name", "description", "steps"],
  "properties": {
    "name": { "type": "string" },
    "description": { "type": "string" },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["step", "agent", "action", "config", "parameters"],
        "properties": {
          "step": { "type": "integer" },
          "agent": {
            "type": "string",
            "description": "Core agents: filesystem, http, llm, tool_executor, webhook, database. Plugin agents also accepted."
          },
          "action": { "type": "string" },
          "config": { "type": "object" },
          "parameters": { "type": "object" },
          "condition": {
            "type": "string",
            "maxLength": 500,
            "description": "Optional expression evaluated against prior step outputs. Step is skipped when the expression evaluates to false."
          },
          "on_error": {
            "type": "string",
            "enum": ["skip", "fail", "retry"],
            "description": "Error recovery strategy: skip (continue workflow), fail (halt), retry (re-attempt with backoff)."
          }
        }
      }
    }
  }
}
